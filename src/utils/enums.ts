enum PageTypes {
  today = "today",
  events = "events",
  tasks = "tasks",
}

enum ItemTypes {
  event = "Event",
  task = "Task",
}

enum subscribtionTypes {
  add = "add",
  edit = "edit",
  delete = "delete",
}

export { PageTypes, ItemTypes, subscribtionTypes };
