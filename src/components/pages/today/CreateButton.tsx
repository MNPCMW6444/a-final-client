import React from "react";
import Button from "@mui/material/Button";

export default function CreateButton() {
  return (
    <div>
      <Button variant="contained">Create a New Item</Button>
    </div>
  );
}
